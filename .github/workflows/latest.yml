# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Build winutils for Apache Hadoop Latest

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Set up JDK 8
      uses: actions/setup-java@v3
      with:
        java-version: '8'
        distribution: 'temurin'

    - name: Add msbuild to PATH
      uses: microsoft/setup-msbuild@v1.1

    - name: Setup Protocol Buffers compiler\
      uses: Noelware/setup-protoc@1.1.0
      with:
        version: "3.7.1"

    # - name: Setup Protocol Buffers
    #   run: |
    #     curl -fsSL -O https://github.com/protocolbuffers/protobuf/releases/download/v3.7.1/protoc-3.7.1-win64.zip
    #     tree
    #     unzip protoc-3.7.1-win64.zip -d protoc

    - name: Test environment
      run: |
        mvn -v
        protoc --version
        cmake --version
        java -version
        MSBuild -ver
        
      
    # - name: Get Hadoop source code
    #   run: curl -fsSL -O https://dlcdn.apache.org/hadoop/common/hadoop-3.3.6/hadoop-3.3.6-src.tar.gz
